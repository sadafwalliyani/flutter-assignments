import 'package:flutter/material.dart';

class bg extends StatelessWidget {
  const bg({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        backgroundColor:Image.asset('assests/image1.JPG').color,
      
    );
  }
}